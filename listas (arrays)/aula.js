const nomes = ['raissa', 'vinicius', 'maria', 'josefa', 'renan'];
const aleatorio = [];

// console.log(nomes[1]);

// adicionar elementos
nomes.push('julia');
nomes[6] = 'igor';

// remove o ultimo elemento
nomes.pop();
// remove o primeiro elemento
nomes.shift();

aleatorio.push('Raissa', 10);
// console.log(aleatorio);

